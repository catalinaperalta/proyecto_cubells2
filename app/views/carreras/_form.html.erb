<%= form_for(@carrera) do |f| %>
  <% if @carrera.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@carrera.errors.count, "error") %> prohibited this carrera from being saved:</h2>

      <ul>
      <% @carrera.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :siglas %><br>
    <%= f.text_field :siglas %>
  </div>
  <div class="field">
    <%= f.label :id_director %><br>
    <%= f.number_field :id_director %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
